- type: entity
  id: ActionBluespacePulse
  name: bluespace pulse
  description: Emit a bluespace pulse that forces phased entities back and stuns them.
  components:
    - type: InstantAction
      icon: { sprite: Interface/Actions/psionics.rsi, state: telekinetic_pulse }
      useDelay: 45
      checkCanInteract: false
      event: !type:BluespacePulseActionEvent
              radius: 6
              stunSeconds: 4
